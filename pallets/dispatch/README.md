# dispatch

Bridge dispatch utilities to route and execute inbound/outbound calls.

## What it does
- Defines common dispatch primitives used by bridge applications.

## Testing
- Run: `cargo test -p dispatch --features runtime-benchmarks`

See root `AGENTS.md` for contribution rules (tests per function and doc-sync).
