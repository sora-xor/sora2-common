# data-signer

Data signing helpers for bridge-related payloads.

## What it does
- Utilities to sign and verify payloads used in bridge messages.

## Testing
- Run: `cargo test -p data-signer --features runtime-benchmarks`

See root `AGENTS.md` for contribution rules (tests per function and doc-sync).
