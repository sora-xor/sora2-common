# liberland-bridge-provider

Provider utilities specific to Liberland bridge integration.

## What it does
- Exposes helpers and types to integrate Liberland-specific bridging flows.

## Testing
- Run: `cargo test -p liberland-bridge-provider --features runtime-benchmarks`

See root `AGENTS.md` for contribution rules (tests per function and doc-sync).
